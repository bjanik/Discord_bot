import ping from "../src/commands/PingCommand"
import discord from "discord.js"
import { expect } from 'chai';

/*

describe('ping', () => {
  it('sends Pong', async () => {
    await ping(onmessage('ping'))
    expect(channel.lastMessage.content).equal('Pong')
  })
})
*/